import styled from "styled-components";

const LoginBox = styled.div`
  width: 500px;
  height: 700px;
  color: white;
  background-color: pink;
`;

export default function Login() {
  return <LoginBox>로그인</LoginBox>;
}
